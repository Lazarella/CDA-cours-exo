import Form from "./form";

const Submit =()=>{

    
const submitHandler =()=>{
}
    return(
<>
        <button className="btn btn-succes" onClick={submitHandler}>Add new Todo</button>
</>
        )
}